import { DisplayFilmsDirective } from './display-films.directive';

describe('DisplayFilmsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayFilmsDirective();
    expect(directive).toBeTruthy();
  });
});
